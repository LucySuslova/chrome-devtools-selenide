# chrome-devtools-selenide

This projects uses Selenide with 'upgrade-to-selenium4-SNAPSHOT' version to test chrome devtools features.


To run tests:
```$xslt
./gradlew clean test
```

Allure report is generated into build/reports/allure-report.